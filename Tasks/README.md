# タスク

このディレクトリは、TODO と課題から参照される具体的な実装・検証作業を管理します。

## ファイルルール

- 1 件のタスクにつき 1 つの Markdown ファイルを作成します。
- `状態`、`優先度`、`Phase`、`QCDS`、`完了条件`、`検証` を記録します。
- `TODO.md` と `Issues/*.md` から Markdown link で参照します。
